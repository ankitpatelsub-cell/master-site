# master-site
AI MASystem unified landing site (port 8099). Fronted by Caddy (masystem.dev) with subpaths to all agents.
Lead form POST /api/lead -> forwards to biz-site (8070).

Caddyfile lives at /etc/caddy/Caddyfile on the VM.
